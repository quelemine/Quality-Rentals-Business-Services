# API Directory - Security & CORS
Options -Indexes

# Block direct access to config files
<Files "database.php">
  Order allow,deny
  Deny from all
</Files>

# Allow CORS preflight
RewriteEngine On
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^(.*)$ $1 [R=200,L]
